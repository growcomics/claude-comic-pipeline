# helpers — the user's accelerator scripts  (B3 feedstock)

Holds the user's own helper / tooling scripts — code that speeds the work
(scrapers, parsers, downloaders, normalizers). **Distinct** from the comic *story*
scripts in `../../scripts-raw/` (those are narrative feedstock; these are tools).

## Status: awaiting delivery

When the user provides them:
1. **Read each** — understand what it does.
2. **Integrate the relevant ones** into the ingestion pipeline — most likely the
   B1 script-ingest (`../ingest_script.py`) or the B2 authenticated-catalog ingest
   (Chrome-MCP blob-fetch path).
3. **Document** which were used and how, in the table below + the corpus README.

Drop scripts here (or point us at a folder path) and we'll wire them in.

| script | what it does | integrated into | notes |
|---|---|---|---|
| _(none received yet)_ | | | |
