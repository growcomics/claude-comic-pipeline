"""Category C — match-directive rules.

These emit short text that POINTS at attached reference images and tells
the model to MATCH them. They never describe what the reference shows
in prose — the reference image IS the description.

If the reference image is wrong, the fix is to regenerate the reference,
not to paraphrase its contents into prompt text every panel.
"""
