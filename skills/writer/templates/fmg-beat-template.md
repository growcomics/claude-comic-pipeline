# FMG Beat-Sheet Template — female muscle growth

*Type `fmg` per `production-briefing/SKILL.md`. Lineup: `muscle-size-lineup.png` (tiers 1–6; extended 4–9 for peak arcs). Chest scales with muscle per the lineup's two-attribute anchor.*

**Region menu** (pick what the concept's transformation actually changes; present multi-region sequences in breasts → glutes → muscles order): `chest`, `hips`, `rear`, `arms`, `abs`, `back`, `shoulders`, `legs`, `suit_fail`, `whole_body`.

**Signature devices**: `multi-panel-progressive` (the FMG gold standard — same region, 3 panels, stage→stage→stage), `sfx-driven`, `clothing-destruction` (seams at shoulders/sleeves/back), `size-comparison` (rival, doorframe, barbell), `reaction-intercut`.

**Feat language**: muscles are *power* — every tier bump buys a feat (a weight that was a wall, now a toy; a grip that dents steel). Feats against objects are warm-ups; feats measured against a *person* (out-lifting, towering over, effortless holds) are climax material.

## Skeleton (N pages; growth floor by CHAPTER-TYPE — transformation ≥60%)

```
ACT 1 — the want (pages 1 … ~0.15N)
  p1        PROMISE: ordinary want stated (the record, the rival's respect, the spot
            on the team). Rival/gauge character established at their fixed tier.
            beats: consider → decide on the engine (vial / device / regimen / gift)

SCENE 1 — first surge (starts by ~0.15N; 3+ growth panels; tiers start→start+1..2)
  beats: trigger → first_sensation → chest → arms  (+ mini reveal)
  devices: sfx-driven + zoom-escalation (+ reaction-intercut if a witness is placed)
  business: sleeves/cuffs biting, first feat (the warm-up weight moves like air)
  face rule: ≤2 consecutive faceless region crops — cut to her flushed/thrilled face

MIDDLE — consequence + escalation (want progresses, situation changes)
  the surge CHANGES something: the rival notices, the plan updates, stakes rise.
  optional SCENE 2 (mid run): arms/abs/back focus, tier +1, a bigger feat,
  a new gauge (doorframe, machine, loaded bar). Each run tops the last.

SCENE FINAL — overload (starts ~0.7–0.8N; 3+ growth panels; tiers → declared end)
  beats: decide/trigger (bigger dose / full power) → chest → hips/rear →
         suit_fail → abs/back → whole_body → reveal (splash-worthy: grid-break
         the page — one full-page image for the peak)
  devices: multi-panel-progressive + clothing-destruction + size-comparison
  staging: depth-staged reveal (lead FG dominant, witness/gauge BG small);
           negative-space-asymmetric for the solo splash

LANDING (last page(s))
  aftermath beat: the want ANSWERED (record board rewritten, rival conceding,
  team spot claimed). One beat of consequence + a closing line. ENDING: landed —
  or a declared HOOK (the rival eyeing the empty vial…).
```

**Tier mapping**: start tier from the concept (typically 2–3), end at the declared peak. Tier ≥6 pulls L29–L32 reinforcement downstream — use for "beyond-bodybuilder" briefs (GrowGetter house scale), deliberately.

**Coverage**: `ALWAYS-CLOTHED` — seams split at shoulders/back/sleeves, hems ride, waistbands strain; coverage always preserved.
